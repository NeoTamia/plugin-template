name: Lint Action

on:
  push:
    branches:
      - main
      - dev
    paths:
      - '**.java'
      - '**.kt'
      - '.github/workflows/lint.yml'
      - '**.gradle'
      - '**.kts'
      - 'gradle/**'
  pull_request:
    branches:
      - main
      - dev
    paths:
      - '**.java'
      - '**.kt'
      - '.github/workflows/lint.yml'
      - '**.gradle'
      - '**.kts'
      - 'gradle/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/v')}}

jobs:
  lint:
    name: Linting
    uses: NeoTamia/actions/.github/workflows/jvm-lint.yml@a9b2cf9e519984fe1b6bf8e654f0c6bb379e4da4 # main
    permissions:
      contents: read
      issues: write
      pull-requests: write
      checks: write
